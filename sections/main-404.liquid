<section class="section">
  <div class="container">
    <div class="reveal" style="text-align:center">
      <p class="eyebrow">404</p>
      <h1 class="h-lg">{{ 'general.404.title' | t | default: 'Page not found' }}</h1>
      <p class="lede muted">This page slipped out of view. Come, we will walk you home.</p>
      <a class="btn" href="{{ routes.root_url }}" data-magnetic="0.2">Come back inside</a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
